# Currículums

Añade aquí los dos documentos manteniendo estos nombres:

- `CV-Samuel-Rodriguez-ES.pdf`
- `CV-Samuel-Rodriguez-EN.pdf`

Después cambia `available` a `true` para cada idioma en `src/cvDocuments.ts`.
